{
  "name": "pageantry",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "yarn bootstrap && yarn --cwd packages/pageantry dev",
    "lint": "eslint .",
    "lint:fix": "yarn lint -- --fix",
    "pretest": "yarn lint",
    "test": "jest"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
